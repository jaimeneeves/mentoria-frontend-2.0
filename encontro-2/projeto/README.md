# ⚙️ Encontro 2 – Arrays, Objetos e Interatividade

> 💡 Manipulando dados e exibindo conteúdo dinâmico.

---

## 🎯 Objetivo

Criar uma **listagem de produtos ou cursos** com botão de filtro.

O objetivo é praticar:
- Criação e manipulação de **arrays e objetos**
- Uso de métodos como `map`, `filter`, `forEach`
- Geração dinâmica de conteúdo no DOM

---

## 🧰 Instruções

1. O HTML e CSS estão prontos.  
2. No **`script.js`**, você deve:
   - Criar um array de objetos (ex: lista de cursos, filmes, produtos etc).
   - Exibir esses itens dinamicamente no HTML.
   - Adicionar botões para **filtrar** os itens por categoria.

---

## 💡 Dica

- Use `innerHTML` para gerar os elementos dentro de uma `<ul>` ou `<div>`.  
- O método `filter()` ajuda a mostrar apenas os itens desejados.  
- Recrie a lista sempre que o filtro mudar.

---

## 📂 Estrutura

```encontro-2/
│── index.html
│── style.css
│── script.js
│── README.md
```
- `index.html`: Estrutura básica da página.
- `style.css`: Estilos para a página.
- `script.js`: Lógica para manipulação de dados e interatividade.
- `README.md`: Documentação do projeto.


---

## 🧠 Desafio Extra

- Adicione um botão “Mostrar tudo”.  
- Adicione um botão “Remover item” em cada card.

---

> 💬 *Esse exercício mostra o poder dos arrays e objetos na geração de conteúdo dinâmico.*
